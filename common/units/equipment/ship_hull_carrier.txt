equipments = {

	ship_hull_carrier = {
		year = 1910

		is_archetype = yes
		is_buildable = no
		type = carrier
		sprite = carrier
		group_by = archetype
		priority = 1000

		interface_category = interface_category_capital_ships

		upgrades = {
			ship_deckspace_upgrade
			carrier_armor_upgrade
			ship_engine_upgrade
			ship_reliability_upgrade
		}

		module_slots = {
			fixed_ship_deck_slot_1 = {
				required = yes
				allowed_module_categories = { ship_deck_space }
				gfx = ship_deck_space_slot
			}
			fixed_ship_deck_slot_2 = {
				required = no
				allowed_module_categories = { ship_deck_space ship_carrier_armor }
				gfx = ship_deck_space_slot
			}
			fixed_ship_engine_slot = {
				required = yes
				allowed_module_categories = { carrier_ship_engine }
			}
			fixed_ship_secondaries_slot = {
				required = no
				allowed_module_categories = { ship_secondaries  ship_dp_secondaries }
			}
			fixed_ship_armor_slot = {
				required = no
				allowed_module_categories = { ship_cruiser_armor }	
			}
			# custom
			mid_1_custom_slot = {
				required = no
				allowed_module_categories = {
					ship_secondaries
					ship_dp_secondaries
				}
			}
		}

		default_modules = {
			fixed_ship_deck_slot_1 = empty
			fixed_ship_deck_slot_2 = empty
			fixed_ship_engine_slot = carrier_ship_engine_1
			fixed_ship_secondaries_slot = empty
			mid_1_custom_slot = empty
		}

		# (Describes combat behaviour - hidden stat)
		offensive_weapons = no

		# class wide stats
		surface_detection = 20
		sub_detection = 1
		surface_visibility = 15   		# same as cruiser at 15
		naval_dominance_factor = 10   	# vanilla is 300, but early carriers were not very effective
		
		naval_weather_penalty_factor = 1 #this value gets -1 before its used so 1.5 will increase the effects of weather penalties by 0.5. this is base value is required for MIO stat modifiers
		#MIO stat modifers will factory by the unmodified stat value so here a 0.5 mio stat modifier would make this ships naval_weather_penalty_factor stat 1.5.
		# change this from 1 at your own risk
	}


	# prototype carrier 1910
	ship_gw_hull_carrier_1 = {		
		year = 1910
		visual_level = 0
		archetype = ship_hull_carrier
		priority = 1900

		module_slots = {
			fixed_ship_deck_slot_1 = inherit
			fixed_ship_deck_slot_2 = inherit
			fixed_ship_engine_slot = inherit
			fixed_ship_secondaries_slot = inherit
			fixed_ship_armor_slot = inherit
			# custom
			mid_1_custom_slot = inherit
		}

		# stats
		naval_range = 4500
		naval_speed = 24  # knots -- early cruisers were quite slow
		fuel_consumption = 20 

		reliability = 0.80
		max_strength = 100
		manpower = 450
		
		resources = {
			steel = 2
		}
		build_cost_ic = 1400  # slightly cheaper than a cruiser
	}


	# first real carriers, 1918
	ship_gw_hull_carrier_2 = {
		year = 1918
		visual_level = 0
		archetype = ship_hull_carrier
		priority = 1900
		parent = ship_gw_hull_carrier_1

		module_slots = {
			fixed_ship_deck_slot_1 = inherit
			fixed_ship_deck_slot_2 = inherit
			fixed_ship_engine_slot = inherit
			fixed_ship_secondaries_slot = inherit
			fixed_ship_armor_slot = inherit
			# custom
			front_1_custom_slot = {
				required = no
				allowed_module_categories = {
					ship_deck_space
					ship_carrier_armor
				}
			}
			mid_1_custom_slot = {
				required = no
				allowed_module_categories = {
					ship_secondaries
					ship_dp_secondaries
					ship_deck_space
					ship_carrier_armor   # added carrier armor and deck space
				}
			}
		}

		# stats
		naval_range = 5500
		naval_speed = 20  # knots -- early cruisers were quite slow
		fuel_consumption = 20 

		reliability = 0.80
		max_strength = 120
		manpower = 550
		
		resources = {
			steel = 2
		}
		build_cost_ic = 1750  # slightly cheaper than a cruiser
	}

}
