wsv_great_war_decisions = {
	
	

	# Austria must decide
	austria_must_decide = {

		icon = generic_decision

		allowed = {
			tag = AUH
		}

		visible = {
			has_global_flag = kis_serbia_refuse
		}

		fire_only_once = yes

		days_remove = 30

		days_re_enable = 0

		remove_effect = {
			set_country_flag = july_ultimatum_expired
			add_stability = -0.2
			add_war_support = -0.25
			add_political_power = -100
		}

		ai_will_do = {
			factor = 0
		}
	}


	# Austria attacks serbia
	austria_invade_serbia = {

		icon = infiltrate_state

		allowed = {
			tag = AUH
		}

		available = {
			NOT = { is_in_faction_with = SER }
		}

		visible = {
			has_global_flag = kis_serbia_refuse
			NOT = { has_country_flag = july_ultimatum_expired }
		}

		fire_only_once = yes

		days_remove = 7

		days_re_enable = 0

		remove_effect = {
			declare_war_on = {
			    target = SER
			    type = puppet_wargoal_focus
			}
			hidden_effect = {
				remove_decision = austria_must_decide
			}
		}

		ai_will_do = {
			factor = 100
		}
	}



	# Schliffen plan
	germany_schliffen_plan = {

		icon = infiltrate_state

		allowed = {
			tag = GER
		}

		available = {
			has_war_with = FRA
		}

		visible = {
			OR = {
				NOT = { has_war_with = BEL }
				NOT = { has_war_with = LUX }
			}
			NOT = { is_in_faction_with = BEL }
			NOT = { is_in_faction_with = LUX }
		}

		fire_only_once = yes

		days_remove = 7

		days_re_enable = 0

		remove_effect = {
			add_timed_idea = {	idea = schlieffen	days = 60	}
			declare_war_on = {
			    target = BEL
			    type = puppet_wargoal_focus
			}
			declare_war_on = {
			    target = LUX
			    type = annex_everything
			}
			if = {
				limit = { ENG = { has_guaranteed = BEL } }
				custom_effect_tooltip = war_with_england_tt
			}
		}

		ai_will_do = {
			factor = 100
		}
	}




	# America attacks
	usa_time_to_strike = {

		icon = infiltrate_state

		allowed = {
			tag = USA
		}

		available = {
			OR = {
				GER = { has_war_with = ENG }
				GER = { has_war_with = FRA }
			}
		}

		visible = {
			NOT = { has_war_with = CSA }
			NOT = { is_in_faction_with = CSA }
		}

		fire_only_once = yes

		days_remove = 7

		days_re_enable = 0

		remove_effect = {
			declare_war_on = {
			    target = CSA
			    type = topple_government
			}
			if = {
				limit = { ENG = { has_guaranteed = CSA } }
				custom_effect_tooltip = war_with_england_tt
			}
		}

		ai_will_do = {
			factor = 100
		}
	}



	join_entente = {

		icon = generic_prepare_civil_war

		allowed = {
			# no stupid asiatic war
			NOT = {
				tag = JAP
				tag = CHI
				tag = QIN
			}
		}

		#available = {
		#
		#}

		visible = {
			is_in_faction = no
			has_war_together_with = FRA
			FRA = { 
				is_faction_leader = yes 
				has_war_with = GER
			}
		}

		days_remove = 4

		days_re_enable = 0

		remove_effect = {
			FRA = {
				add_to_faction = ROOT
			}
		}

		ai_will_do = {
			factor = 10
		}
	}


	join_central_powers = {

		icon = generic_prepare_civil_war

		allowed = {
			# no stupid asiatic war
			NOT = {
				tag = JAP
				tag = CHI
				tag = QIN
			}
		}

		#available = {
		#
		#}

		visible = {
			is_in_faction = no
			has_war_together_with = GER
			GER = { 
				is_faction_leader = yes 
				has_war_with = FRA
			}
		}

		days_remove = 4

		days_re_enable = 0

		remove_effect = {
			GER = {
				add_to_faction = ROOT
			}
		}

		ai_will_do = {
			factor = 10
		}
	}
}