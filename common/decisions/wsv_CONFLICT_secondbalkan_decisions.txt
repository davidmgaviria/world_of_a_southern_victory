secondbalkan_war_cat = {

	## Bulgaria war countdown
	secondbalkan_countdown_to_war = {
		icon = generic_ignite_civil_war

		allowed = {
			original_tag = BUL
		}
		activation = {
			has_global_flag = BUL_prepares_against_serbia_over_macedonia
			NOT = { has_country_flag = secondbalkan_war_prep_over }
		}
		available = {
			always = no
		}
		cancel_trigger = {
			has_full_control_of_state = 106
		}
		cancel_effect = {
			set_country_flag = secondbalkan_war_prep_over
		}
		
		selectable_mission = no
		days_mission_timeout = 24
		is_good = no
		
		timeout_effect = {
			set_country_flag = secondbalkan_war_prep_over
			news_event = wsv_secondbalkan.15
		}
	}

	# Serbia can stop the war
	secondbalkan_serbia_prevent_war = {
		icon = generic_form_nation

		allowed = {
			original_tag = SER
		}
		visible = {
			has_global_flag = BUL_prepares_against_serbia_over_macedonia
			NOT = { has_country_flag = secondbalkan_war_prep_over }	
			NOT = { has_war_with = BUL }
		}
		available = {
			has_global_flag = austria_ultimatum_over_albania
			has_full_control_of_state = 759
		}

		days_remove = 3

		complete_effect = {
			add_political_power = -75
			BUL = { 
				effect_tooltip = { transfer_state = 759 }
				country_event = wsv_secondbalkan.10 
			}
			custom_effect_tooltip = bulgaria_will_halt_war_prep_tt
		}

		ai_will_do = {
			factor = 100
		}
	}
}